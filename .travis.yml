sudo: required

services:
  - docker

cache:
  directories:
    - $HOME/arrow_docker_cache

env:
  global:
    - DOCKER_COMPOSE_VERSION=1.22.0
    - ARROW_DOCKER_CACHE_DIR=$HOME/arrow_docker_cache

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - docker -v
  - docker-compose -v

before_script:
  - git clone -b ARROW-3506 https://github.com/kszucs/arrow arrow
  - cd arrow
  - git checkout ec47011e28548c94558ecc44d6448b96c21915b6
  - git submodule update --init --recursive

script:
  - docker-compose build cpp
  - docker-compose run cpp